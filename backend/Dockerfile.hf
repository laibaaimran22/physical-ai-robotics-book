FROM python:3.10-slim

WORKDIR /app

# Use the Hugging Face optimized requirements
COPY requirements_hf_spaces.txt .
RUN pip install --no-cache-dir -r requirements_hf_spaces.txt

COPY ./src ./src

EXPOSE 7860

CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "7860"]